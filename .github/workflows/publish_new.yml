name: PUBLISH NEW

on:
  push:
    branches: [ master ]

jobs:
  publish:
    if: contains(github.event.labels.*.name, 'PUBLISH')
    runs-on: ubuntu-latest
    steps:
    - name: Publish New
      run: |
        echo 'Publish New'
      shell: bash
